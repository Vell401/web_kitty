version: '3.8'

services:
  # База данных
  postgres:
    image: postgres:13
    env_file: .env
    volumes:
      - pg_data:/var/lib/postgresql/data

  # Бэкенд приложение
  backend:
    image: vell401/kittygram_backend:latest  # ⚠️ Твой Docker Hub username
    env_file: .env
    depends_on:
      - postgres
    volumes:
      - static:/app/static/
      - media:/app/media/

  # Фронтенд
  frontend:
    image: vell401/kittygram_frontend:latest
    command: cp -r /app/build/. /static/
    volumes:
      - static:/static/

  # Nginx (Gateway)
  gateway:
    image: vell401/kittygram_gateway:latest
    ports:
      - "9000:80"
    volumes:
      - static:/staticfiles/
      - media:/media/
    depends_on:
      - backend
      - frontend

volumes:
  pg_data:
  static:
  media: